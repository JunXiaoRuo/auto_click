# 更新日志

## v2.3 - 2026-08-03

- 全面重做主界面与各项弹窗，统一使用 Material 风格，并完善浅色、深色及横屏双栏布局。
- 标题版本号改为自动读取构建配置，后续升级版本无需再手动修改界面文案。
- 普通点击新增“快速点击”和“自定义按压”两种模式；点击持续时间只在自定义按压模式下生效。
- 新增默认关闭的实验性高速模式，支持触摸设备自动识别、执行前自检、异常强制释放及失败自动回退。
- 随机坐标偏移和随机延迟改为范围设置，每次执行都会在所设范围内重新取值。
- 新增点击轨迹显示，可通过颜色选择器设置常用色、HEX 颜色及透明度；轨迹显示不再阻塞实际点击。
- 移除执行界面的循环间隔设置，循环等待统一由首个动作延迟控制，并将默认等待调整为 0 ms。
- 首次启动自动解析触摸设备，减少录制前的手动配置步骤。
- 优化步骤列表、空状态、权限状态、Root 检查、应用排序、悬浮球状态和通知权限处理。
- 修复粘性服务收到空 Intent 时可能误开录制悬浮球的问题，并加强执行终止及资源回收。

## 2026-05-24

- 修复横屏录制时触摸坐标映射不准的问题：录制器现在会按屏幕旋转方向转换 `getevent` 原始坐标。
- 在设置中新增“坐标旋转修正”选项，支持“自动修正”“关闭修正”“横屏反向修正”，便于适配不同机型的横屏坐标方向。
- 录制服务会把坐标修正模式传给录制器，确保开始录制时按当前设置生效。
